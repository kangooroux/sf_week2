{% extends "_template.html.twig" %}

{% block title %}{{ label_name|upper }}{% endblock %}

{% block content %}
    <div class="section">
        <h1 class="title">{{ label_name|upper }}</h1>
        <h2 class="subtitle">Albums: {{ label_records|length }}</h2>

        {% include 'record/_record_list.html.twig' with {record_list: label_records} %}
    </div>

{% endblock %}